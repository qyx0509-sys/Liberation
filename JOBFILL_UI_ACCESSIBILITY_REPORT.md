# 解放 · 基础无障碍检查

## 本轮实现

- Popup、Options 与 Shadow DOM 共用 `:focus-visible` 焦点环及 reduced-motion 规则。
- 图标按钮具备可访问名称；装饰性 SVG 使用 `aria-hidden` 和 `focusable="false"`。
- 网页助手使用 tablist／tab／tabpanel、aria-selected 及键盘切换。
- 高级菜单、导出菜单、抽屉和确认对话框支持 Escape；关闭后归还焦点。
- 模态确认使用 aria-modal、标题关联及焦点限制。关闭或取消不等于授予填写／上传权限。
- Options 自动关联普通及动态重复字段的 label；长文本与 GPA 帮助关联到控件。
- 保存和任务状态使用礼貌播报，必要错误使用 assertive。
- 主要控件保持约 36–40px 触达尺寸，不依赖颜色单独表达成功、缺失或错误。

## 已验证的行为

真实浏览器测试覆盖 Popup 高级菜单 Escape 与焦点归还、Options 抽屉在 1024/390 宽度的开启关闭、缺失字段定位、导出菜单 Escape、未保存离开保护、保存错误重试、无效导入、重复记录移动以及删除取消／确认。网页助手的语义 Tab、状态操作及模态行为还有专门的单元／浏览器回归。

静态与运行时契约检查覆盖本地资源、按钮名称、表单标签关联、重复 ID、加载顺序、ARIA 属性和统一样式。没有为检查安装第三方框架或无障碍依赖。

## 对比度

单元测试分别计算浅色／暗色的主要、次要、弱化文字和状态文字配色，要求文本对比度至少 4.5:1；强调控件边界与表面至少 3:1。浅色 `border-strong` 调整为 `#7A8CA3`，避免仅追求浅边框而牺牲可辨识度。

## 边界说明

这是基础 WCAG AA 目标检查，不是完整合规认证。没有进行屏幕阅读器用户测试、所有浏览器／操作系统组合测试，也未覆盖任意第三方学校页面自身的无障碍问题。网页助手只控制自己的隔离 UI，不重写学校页面语义。

完整命令与最终结果见 `JOBFILL_UI_TEST_RESULT.txt`；截图及逐项浏览器检查见 `ui-review/`。
