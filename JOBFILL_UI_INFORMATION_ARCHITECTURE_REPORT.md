# 解放 · UI 信息架构

本轮以用户提供的 Product-grade Interface Redesign 规格为准；维持 Vanilla HTML/CSS/JS、MV3、本地资源及现有资料格式。

## 三个界面的职责

| 界面 | 主要信息 | 主要操作 | 次级入口 |
| --- | --- | --- | --- |
| Popup | 当前页面、可处理字段、资料覆盖、任务状态、最多三条待确认摘要 | 随状态变化的唯一主操作 | 高级菜单、仅当前页、打开网页助手 |
| 网页助手 | 当前任务、进度、最近字段、待确认和运行日志 | 开始／暂停／继续／处理待确认／结果 | 设置、材料库、重新检查、重跑、诊断 |
| 我的申请资料 | 个人资料、重复经历、材料、当前模块及缺失字段 | 保存资料 | 导入、导出、分组导航、资料检查器 |

Popup 不再重复铺开全部控制按钮。网页助手保持 Shadow DOM，采用“任务／待确认／日志”三个语义化 Tab；普通待确认界面仅消费字段名称、栏目、状态及产品化原因。

## 状态与数据来源

- `src/ui/design-system.js` 提供状态文案、图标、浅色／暗色变量与交互基础样式。
- `src/ui/design-tokens.css` 由现有构建脚本从同一份变量生成；Shadow DOM 直接使用 `JFUI.tokensCSS`，避免三套调色板。
- `src/ui/review-presenter.js` 从同 scanId 的报告或扫描结果显式重建元数据白名单，不传递填写值、原始 reason 或 optionMatchingDebug。
- 恢复任务只有持久化统计时，展示汇总及重新检查提示，不假装仍有旧字段明细。新报告优先于旧扫描结果。
- ContentController 只新增 UI 投影及既有操作的界面路由；匹配、执行、授权、保存和安全判断均保持原模块。
- 已知任务队列按已处理栏目计算整体进度并标明单位；不猜测尚未打开页面的字段总数。

## 资料工作台

导航分为个人、教育、经历、科研、能力与荣誉、申请补充。原 `sec-*` 锚点与控件 ID 继续兼容；旧版 work 编辑器归入“更多／兼容数据”，默认折叠，不再与学习和工作经历并列。

表单以两列为主，长地址与长文本占整行。教育、经历、科研、实践、奖励、语言、论文、家庭记录采用一致的标题、摘要、移动、删除及添加布局。移动 DOM 节点保留记录身份与不可编辑的附件等元数据；删除必须明确确认。

检查器显示整体覆盖、当前模块统计及最多五个可定位的缺失字段。覆盖度分母为当前资料中受管理的可编辑字段，包括现有重复记录和补充字段；不把未知 JSON、材料二进制、资料名称、导入操作框或不存在的经历记录当成学校必填项。

## 保存与兼容性

Dirty、Saving、Saved、SaveError、Importing、ImportError 都是页面内状态，不新增 storage schema。未保存时才启用离开提醒；保存失败保留编辑内容；保存进行中再次编辑不会被误标为已保存。

保留既有 JSON／MD／TXT／DOCX／PDF 本地导入与 JSON／MD 导出。为维持原行为，成功导入仍立即保存，显示“导入已保存，请检查资料”，而不是伪造未保存状态。无效导入不改旧资料。

照片继续使用原材料库、证件照分类及绑定；没有新增上传系统、学校尺寸硬限制或图片处理。

## 响应式与资源

桌面三栏；中等宽度保留导航和主表单、检查器改抽屉；小屏使用顶部栏目选择器与单列表单。Popup 根节点以 width／min-width 明确请求 420px，不再用 100vw 限制自身初始测量；最大 600px 高，长内容只在 main 内滚动，头部与底栏保持可见。网页助手默认 380×544、最小约 320×360，保留拖拽、缩放、最小化与隐藏。

图标全部来自本地内联线性 SVG，系统字体，不添加框架、字体下载或 CDN。设计概念仅是交付对照图，不参与扩展运行。
