# 解放 · 视觉与真实浏览器回归

## 验证环境与方式

Browser plugin not available。遵循前端测试技能的备用路径，复用项目已有的 Edge／CDP E2E 工具，没有安装 Playwright 或其他新依赖。测试使用独立临时浏览器资料目录、本地 HTML 夹具与虚构资料；另有真实 MV3 扩展加载、Service Worker 和 Popup 测试。

主要流程：入口加载 → 页面识别及本地资料回填 → 状态变化或用户编辑 → 可见按钮／保存状态／待确认内容正确更新。检查页面身份、非空内容、无错误遮罩、控制台健康、无外部资源、无横向溢出、无重复 ID 和无名按钮。

截图由 `tests/e2e/ui-final-polish.e2e.mjs` 生成。截图前等待 240ms，让本地短颜色过渡结束，避免将暗色切换中间帧误判为最终样式。截图是审阅证据，不是跨平台逐像素 golden-image 测试。

## 截图范围

| 界面 | 视口／状态 | 文件 |
| --- | --- | --- |
| Popup | 420×600 的本地页面补充截图，空资料、就绪、运行、暂停、待确认、结束、错误、暗色 | `popup-empty/ready/running/paused/review/finished/error/dark.png` |
| 网页助手 | 1100×800 页面中的 380×544 面板：任务、待确认、日志、暗色 | `floating-task/review/log/dark.png` |
| 状态球 | 最小化，进度环与待确认计数 | `launcher.png` |
| 资料工作台 | 1440、1024、768、390 宽度以及暗色 | `options-1440/1024/768/mobile/dark.png` |

逐张检查元数据写入 `ui-review/ui-checks.json`；最终命令状态及构建一致性记录在 `ui-review/verification.json`。

## 概念对照与修正记录

设计概念保留为 `ui-review/concept-popup-floating.png` 和 `ui-review/concept-options.png`，不作为插件内的图片 UI。

| 比较点 | 对照与实现 |
| --- | --- |
| 信息架构 | 保留轻量头部、当前页面、清晰主操作；资料页为导航／编辑／检查器三栏。 |
| 主色与表面 | 使用统一青蓝色、白色表面及冷灰背景，没有照搬概念图的背景装饰。 |
| 状态色 | 按用户文字要求，将概念图中红色“资料缺失”改为中性；危险色只用于真正错误。 |
| 文案与数据 | 不复制概念数字或擅自新增必填星号；使用现有真实资料和扫描统计，保留地区／地址独立。 |
| 首屏密度 | 修正 Popup 运行态资料区过高的问题；当前主要操作及最多三条待确认摘要可见。 |
| 字体与图标 | 系统字体、本地统一线性 SVG；无大面积 9px 正文、无 Emoji 主图标。 |
| 响应式 | 中等宽度检查器抽屉、小屏两行顶栏及单列表单；不将桌面三栏强行压缩到手机。 |
| 暗色 | 共用暗色变量；修正 Shadow host 的 color-scheme 重置与滚动条白底。 |

以上差异是对用户正式规格的落实，不是对示意图假数据或装饰的机械复刻。主代理已使用本地图片查看工具对照概念和浏览器截图；最终截图还需与最终测试记录一同阅读。

## 交互中发现并修正的问题

- 奖励测试样例缺少原验证规则要求的日期：补正虚构样例，未放宽保存验证。
- 删除确认框快速取消后重开：旧原生 close 事件可能结算新会话，改为独立会话的明确操作结算，并反复验证。
- Popup 运行态冗余统计挤压待确认摘要：调整活动状态的信息密度，保留真实覆盖数据。
- 成功扫描后的旧提示可能与新任务状态冲突：清理重复过时提示。
- file:// Popup 测试关闭普通窗口会产生浏览器提示：测试替身记录关闭意图；真实扩展 Popup 仍由独立加载测试验证。
- 真实扩展 Popup 的测试曾依赖桌面前台窗口：改为精确定位隔离测试窗口并向 openPopup 显式传入 windowId，两次独立运行通过，不修改产品逻辑或弱化断言。

## Popup 原生尺寸回归修复

用户实际点击扩展图标后反馈窄长条布局。复现证明 html／body 的 `width:420px` 被 `max-width:100vw` 限制到 Popup 初始窄视口；根文档和正文双重滚动进一步缩窄内容。旧版实际 viewport 为 146px、body 为 131px，标题逐字换行，主按钮落在首屏外。

此前 420×640 截图使用 file 页面和手动视口；真实 MV3 测试只检查成功加载，没有检查尺寸，因此未发现该缺陷。不能以那些截图证明原生 Popup 自动定宽正确。

现在根节点明确设置 width／min-width 为 420px，移除视口宽度上限；body 高度限制为 600px，由 main 弹性收缩并内部滚动，高级菜单受实际可用高度限制。仅修改 Popup 样式及相关测试，没有修改填写、资料或授权逻辑。

新增 `extension-load.e2e.mjs` 原生尺寸用例，不在 Popup target 上调用 device metrics override。断言实际根宽度、单行标题、主按钮首屏可见、无根横纵溢出、底栏固定、长内容滚动，以及真实菜单点击与 Escape 焦点归还。该用例已在旧 dist 上准确失败；`JF_POPUP_DEVICE_SCALE_FACTOR=2` 可复现 200% 设备比例，而不是人为给 Popup 一个正确视口。原有多状态截图同步使用 420×600 作为补充。

## 已知边界

已覆盖列出的本地浏览器与视口，不代表所有学校站点、所有缩放设置、所有操作系统字体或所有辅助技术组合均已认证。未登录学校、未使用真实申请资料，也未触发学校页面的下一步或最终提交。UI 测试场景中的显式记录删除仅针对虚构本地资料。
